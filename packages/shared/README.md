# `packages/shared` Scaffold

Shared contracts and utilities used across Electron and API modules.

Wave 0 provides baseline JSON schema contracts for:
- event envelopes
- API error payloads

Downstream modules should version contract files and update producer + consumer + validation + tests together.
